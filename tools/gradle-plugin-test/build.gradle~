buildscript {
    repositories {
	mavenCentral()
	mavenLocal()
    }

    dependencies {
        classpath 'pl.sebitg:gradleplugin:0.2.+'
    }
}

apply plugin: 'java'
apply plugin: 'testandpmdresultssender'

tutorialArgs {
    password = externalProps['mail.password']
    auth = externalProps['mail.auth']
}

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
	compile('com.dupa:cos:1.0')
}
